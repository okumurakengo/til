"use strict";

// index.js (main process)
// - GUI (renderer process)
// - GUI (renderer process)
// - GUI (renderer process)

const electron = require("electron");
const app = electron.app;

app.on("ready", () => {

});
